import { RespSerializerInterceptor } from './resp-serializer.interceptor';

describe('RespSerializerInterceptor', () => {
  it('should be defined', () => {
    expect(new RespSerializerInterceptor()).toBeDefined();
  });
});
